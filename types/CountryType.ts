type CountryType = {
  name: {
    common: string;
    official: string;
  };
  flags: {
    png: string;
  };
  population: string;
  region: string;
  capital: string;
};

export default CountryType;
